package cutImages;

public class IconIncise {
  /*  public static void main(String[] args) {
        String fileName = "C:\\Users\\Administrator\\Desktop\\aaaaa\\1111_2.jpg";
        BufferedImage sourceImg = null;
        try {
            sourceImg = ImageIO.read(new FileInputStream(fileName));  // 读取图片的地址
        } catch (IOException e) {
            e.printStackTrace();
        }
        String suffix = fileName.substring(fileName.lastIndexOf(".") + 1);
        assert sourceImg != null;
        int width = sourceImg.getWidth();    // 获取图片的 宽度
        int height = sourceImg.getHeight(); // 获取图片的 高度
        int x = sourceImg.getMinX();
        if (height < 400){
            Test01.cutPicture(suffix, fileName, x, getInt(height * 0.2), width-20, getInt(height * 0.65), "C:\\Users\\Administrator\\Desktop\\xxxx\\1111_1.jpg");
        } else {
            Test01.cutPicture(suffix, fileName, x, getInt(height * 0.2), width-20, getInt(height * 0.50)-20, "C:\\Users\\Administrator\\Desktop\\xxxx\\1111_1.jpg");
        }

    }

    //四舍五入把double转化int整型，0.5进一，小于0.5不进一
    private static int getInt(double number) {
        BigDecimal bd = new BigDecimal(number).setScale(0, BigDecimal.ROUND_HALF_UP);
        return Integer.parseInt(bd.toString());
    }*/
}
